# Program to check if the number is a prime or not.

def prime_num(num):
    temp = 0
    for i in range(1,num+1):
        if num%i == 0:
            temp = temp+1
            if temp > 2:
                break
    if temp==2:
        
        print ("{} is a Prime Number".format(num) )
        
    else:
        print ("{} is not a prime Number".format(num))
            

            
prime_num(11)
         
